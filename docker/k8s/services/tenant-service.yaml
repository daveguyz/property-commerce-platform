# tenant-service — Deployment + Service + HorizontalPodAutoscaler
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-service
  namespace: pcp
  labels: { app: tenant-service }
spec:
  replicas: 1
  selector:
    matchLabels: { app: tenant-service }
  template:
    metadata:
      labels: { app: tenant-service }
    spec:
      containers:
        - name: tenant-service
          image: ghcr.io/daveguyz/property-commerce-platform/tenant-service:latest
          ports:
            - containerPort: 8096
          envFrom:
            - configMapRef: { name: pcp-common-config }
            - secretRef:    { name: pcp-secrets }
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: "1",  memory: 1Gi }
          livenessProbe:
            httpGet: { path: /actuator/health/liveness,  port: 8096 }
            initialDelaySeconds: 60
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8096 }
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: pcp
spec:
  selector: { app: tenant-service }
  ports:
    - port: 8096
      targetPort: 8096
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: tenant-service
  namespace: pcp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: tenant-service
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
