# auction-service — Deployment + Service + HorizontalPodAutoscaler
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auction-service
  namespace: pcp
  labels: { app: auction-service }
spec:
  replicas: 3
  selector:
    matchLabels: { app: auction-service }
  template:
    metadata:
      labels: { app: auction-service }
    spec:
      containers:
        - name: auction-service
          image: ghcr.io/daveguyz/property-commerce-platform/auction-service:latest
          ports:
            - containerPort: 8094
          envFrom:
            - configMapRef: { name: pcp-common-config }
            - secretRef:    { name: pcp-secrets }
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: "1",  memory: 1Gi }
          livenessProbe:
            httpGet: { path: /actuator/health/liveness,  port: 8094 }
            initialDelaySeconds: 60
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8094 }
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: auction-service
  namespace: pcp
spec:
  selector: { app: auction-service }
  ports:
    - port: 8094
      targetPort: 8094
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: auction-service
  namespace: pcp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: auction-service
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
