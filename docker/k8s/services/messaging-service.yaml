# messaging-service — Deployment + Service + HorizontalPodAutoscaler
apiVersion: apps/v1
kind: Deployment
metadata:
  name: messaging-service
  namespace: pcp
  labels: { app: messaging-service }
spec:
  replicas: 1
  selector:
    matchLabels: { app: messaging-service }
  template:
    metadata:
      labels: { app: messaging-service }
    spec:
      containers:
        - name: messaging-service
          image: ghcr.io/daveguyz/property-commerce-platform/messaging-service:latest
          ports:
            - containerPort: 8093
          envFrom:
            - configMapRef: { name: pcp-common-config }
            - secretRef:    { name: pcp-secrets }
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: "1",  memory: 1Gi }
          livenessProbe:
            httpGet: { path: /actuator/health/liveness,  port: 8093 }
            initialDelaySeconds: 60
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8093 }
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: messaging-service
  namespace: pcp
spec:
  selector: { app: messaging-service }
  ports:
    - port: 8093
      targetPort: 8093
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: messaging-service
  namespace: pcp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: messaging-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
