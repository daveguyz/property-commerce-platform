# pricing-engine — Deployment + Service + HorizontalPodAutoscaler
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pricing-engine
  namespace: pcp
  labels: { app: pricing-engine }
spec:
  replicas: 1
  selector:
    matchLabels: { app: pricing-engine }
  template:
    metadata:
      labels: { app: pricing-engine }
    spec:
      containers:
        - name: pricing-engine
          image: ghcr.io/daveguyz/property-commerce-platform/pricing-engine:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef: { name: pcp-common-config }
            - secretRef:    { name: pcp-secrets }
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: "1",  memory: 1Gi }
          livenessProbe:
            httpGet: { path: /actuator/health/liveness,  port: 8085 }
            initialDelaySeconds: 60
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8085 }
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: pricing-engine
  namespace: pcp
spec:
  selector: { app: pricing-engine }
  ports:
    - port: 8085
      targetPort: 8085
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pricing-engine
  namespace: pcp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pricing-engine
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
