# trust-service — Deployment + Service + HorizontalPodAutoscaler
apiVersion: apps/v1
kind: Deployment
metadata:
  name: trust-service
  namespace: pcp
  labels: { app: trust-service }
spec:
  replicas: 1
  selector:
    matchLabels: { app: trust-service }
  template:
    metadata:
      labels: { app: trust-service }
    spec:
      containers:
        - name: trust-service
          image: ghcr.io/daveguyz/property-commerce-platform/trust-service:latest
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef: { name: pcp-common-config }
            - secretRef:    { name: pcp-secrets }
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: "1",  memory: 1Gi }
          livenessProbe:
            httpGet: { path: /actuator/health/liveness,  port: 8086 }
            initialDelaySeconds: 60
            periodSeconds: 15
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8086 }
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: trust-service
  namespace: pcp
spec:
  selector: { app: trust-service }
  ports:
    - port: 8086
      targetPort: 8086
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: trust-service
  namespace: pcp
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: trust-service
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
