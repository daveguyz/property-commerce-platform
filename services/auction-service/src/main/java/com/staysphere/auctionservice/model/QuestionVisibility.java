package com.staysphere.auctionservice.model;

public enum QuestionVisibility {
    PRIVATE,  // only the bidder and auctioneer can see the answer
    PUBLIC    // answer broadcast to all room subscribers
}
